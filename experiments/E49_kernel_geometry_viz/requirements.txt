# Core dependencies for Surjection→QEC + Fractional Laplacian experiments (E49-E56)
# Compatible with Mac M1, Python 3.9+

# Numerical computation
numpy>=1.24.3
scipy>=1.11.1

# Visualization
matplotlib>=3.7.2

# Testing framework
pytest>=7.4.0
pytest-cov>=4.1.0

# Optional: For E52 (QEC benchmarks) - HEAVY, install only if needed
# qiskit>=0.44.0  # ~500MB, not required for surjection approach

# Optional: For E55 (random surfaces) - install only if needed
# scikit-learn>=1.3.0  # For Delaunay triangulation

# Configuration
pyyaml>=6.0.1

# Data validation
jsonschema>=4.19.0
